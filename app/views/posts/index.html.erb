<div class="row justify-content-center">
    <div class="col-6 col-offset-3">
        <h1>posts index</h1>
        <% if logged_in? %>
            <div class="mb-4">
                <%= render 'post_form' %>
            </div>
        <% end %>
        <div class="d-flex flex-row justify-content-between align-self-stretch flex-wrap">
            <%= render @posts %>
        </div>
        <%= will_paginate(@posts, :renderer => WillPaginate::ActionView::Bootstrap4LinkRenderer) %>
    </div>
</div>
